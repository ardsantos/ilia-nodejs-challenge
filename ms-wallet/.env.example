# Local Development Environment for ms-wallet
# Copy this file to .env and adjust values as needed

# Database connection (use localhost when running service locally with Docker databases)
# Port 5433 connects to the postgres-wallet container from docker-compose.yml
DATABASE_URL=postgresql://user_wlt:your_wallet_db_password@localhost:5433/wallet_db

# JWT Secrets (CHANGE THESE IN PRODUCTION!)
JWT_SECRET=dev_jwt_secret_change_in_production
JWT_SECRET_INTERNAL=dev_internal_jwt_secret_change_in_production

# Service port
PORT=3001
